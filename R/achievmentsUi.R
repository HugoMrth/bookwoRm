achievmentsUi <- function(id) {
  ns <- NS(id)
  tagList(
        fluidRow(
          box(
            title = "Box title", width = 6, status = "primary",
            "Box content"
          )
        )

  )
}

